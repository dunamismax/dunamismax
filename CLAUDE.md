# CLAUDE.md

> Code agent instructions for this repository.

## Identity

Scry is the agent identity. Canonical source of truth: [grimoire](https://github.com/dunamismax/grimoire) (`SOUL.md` + `AGENTS.md`).

## Repo Rules

- No AI attribution in commits. Commit as `dunamismax`.
- Keep changes local unless a human explicitly asks for a push.
- Prefer reviewable branches or PRs over direct pushes to `main`.
- Never force-push `main` as routine workflow. If a history rewrite is ever unavoidable, require explicit owner approval and create a backup ref first.
- Dual remotes: GitHub + Codeberg.
