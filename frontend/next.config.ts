/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'via.placeholder.com',
      },
      // Add other image hosts if needed for your photography portfolio
    ],
  },
};

export default nextConfig;